# CLAUDE.md

## このリポジトリは何か
株式会社タイガーラボの生成AI活用支援サービスのランディングページ（LP）。
1ページ完結の静的サイトで、ビルド工程は一切なし。GitHub Pages で公開予定だが**公開URLは未確定**。
オーナーは非エンジニアのため、変更は小さく・説明的に・壊さないことを最優先する。

## ファイル構成（LP本体は3ファイルのみ）
- `index.html` — LP本体。CSS・JS・favicon（SVG data URI）・JSON-LD をすべてインライン保持
- `robots.txt` — 全クローラー許可。Sitemap 行はURL確定待ちで意図的にコメントアウト中
- `sitemap.xml` — `<loc>` は「https://あなたの公開URL/」のプレースホルダのまま（意図的）
- `docs/` — 監査レポート等のドキュメント（サイトとしては配信されない前提の資料置き場）

## 編集時の不変条件
- コンテンツはすべて日本語。中小企業経営者向けの丁寧なトーンを維持する
- 単一HTML・ビルド無し・**外部リクエストゼロ**の方針を守る。CDN・Webフォント・外部CSS/JS・アナリティクスタグを勝手に追加しない
- JSON-LD（ProfessionalService / FAQPage）とページ可視コンテンツは常に一致させる。FAQ・サービス名・料金・会社名を変更したら、本文と JSON-LD の**両方を同時に**更新する
- `index.html` の内容を変更したら `sitemap.xml` の `<lastmod>` を当日日付に更新する
- 「あなたの公開URL」プレースホルダ（canonical / og:url / sitemap / robots.txt）は公開URL確定まで意図的に残している。**架空のURLで勝手に埋めない**。差し替え時は `/publish-check` スキルを使う
- 問い合わせCTA「受付フォームを準備中です」（`href="#"` + `aria-disabled`）も意図的な暫定状態。フォームURL確定まで有効化しない
- 料金は「月額30,000円〜」。数値・実績（11社・144,000分）は事実に基づくため、根拠なく変更・誇張しない
- 画像を追加する場合は width/height 属性を必ず指定し、ファーストビュー外は `loading="lazy"`。OGP画像は絶対URLでのみ指定可
- アクセシビリティ方針を踏襲する：ピンチズーム許可・装飾要素（絵文字・引用符・SVG）への `aria-hidden="true"`・フォーカスアウトライン維持・`prefers-reduced-motion` への配慮

## 動作確認
ブラウザで `index.html` を直接開けば確認できる（サーバ不要）。
必要なら `python3 -m http.server 8000` でローカル配信も可。
変更後は `grep -n 'あなたの公開URL' index.html robots.txt sitemap.xml` で意図しないプレースホルダ操作がないか確認する。

## Git 運用
- コミットメッセージは変更内容と意図が分かる日本語で書く（例:「料金セクションに税込表記を追加」）。「Add files via upload」のような無意味な文言は禁止
- main へ直接コミットせず、作業ブランチ → PR でマージする
- `index.html` の丸ごと上書き（全置換）は禁止。必ず差分ベースで編集する（過去に上書きアップロードで約1,400行の改稿が消失した事故あり。失われた版は `git show 188d531:index.html` で復元可能）
