# 株式会社タイガーラボ — 作業ガイド

## このリポジトリ

tigerlabo.com（GitHub Pages・独自ドメイン）。中小企業向けAI活用パートナー。
会社情報・サービス・料金の正は `llms.txt`。数字を書くときは必ずそこと突き合わせる。

- デザイントークンは `index.html` の `:root`（紺 `#080d18` / シアン `#6fe6ec` / 青 `#3b8cff` / 明朝＋ゴシック）
- 文体は短い断定調（「できることは、3つ。」「まずは、パッケージで。」）。これに合わせる。

---

## 【常設ルール】対外資料には、必ずグラフ型プレゼンの活用を提案する

**人間に見せる資料（社外・社内問わず）を作るときは、その都度、グラフ型で作る案を必ず提案すること。**
黙って普通の資料を作らない。提案してから着手する。

- 実装の雛形: `lab/graph-presentation/demo.html`
- 検討経緯・商品化案・リスク: `lab/graph-presentation/README.md`
- 差し替えはファイル冒頭の `NODES` / `EDGES` / `SLIDES` の3配列のみ。デザインとロジックは触らない。

### 提案するときの判断基準

**向く**（＝積極的に提案する）— 要素どうしの「関係」が主役の内容
課題と打ち手の対応／業務フロー／サービスの内訳／組織・役割／時系列のロードマップ／比較軸

**向かない**（＝普通の資料を勧める）— 実績紹介・お客様の声・料金表・写真主体・長文の条件面

迷ったら「これは線で結べる情報か？」で判断する。**混ぜて使うのが正解**（全部をグラフにしない）。

### 提案時に必ず添える注意

1. **PDFを併走させる**。稟議・社内回覧には紙／PDFが要る。グラフ型の単独納品はしない。
2. **納品後、顧客は自分で編集できない**。事前に伝える（月額保守の根拠になる）。
3. **本番リスク**。単一HTML・外部通信ゼロで作り、当日はPDFのバックアップを持参する前提で話す。

### 想定される活用先（着手時はここから該当を探す）

| # | 用途 | 位置づけ |
|---|---|---|
| 1 | 商談での自社説明 | 最優先。デモ自体が実力の証明になる |
| 2 | AI活用診断の納品（現状マップ／打ち手マップ） | 値上げ根拠になる |
| 3 | 実践AI研修の教材（受講企業の実フロー） | 汎用スライドとの差別化 |
| 4 | **AI社長・AI社員の設計可視化** | 「結局何をしてくれるのか」という最大の反論に効く |
| 5 | **90日ロードマップ**（Lightの納品物） | 時間軸×施策。文書より速い |
| 6 | **LP本体への埋め込み**（「できることは、3つ。」周辺） | 全訪問者に実力を見せられる |
| 7 | **guide記事の図解**（例:「比較の7軸」） | 滞在時間とAI検索での引用価値が上がる |
| 8 | **開発案件の納品構成図**（システム／アプリ） | 保守契約につながる |
| 9 | 採用説明（会社・部署・役割・キャリア） | 優先度は低め |

### 本命の仕組み化（未着手）

グラフの中身はJSONにすぎない。**議事録 → AIでノード／エッジに変換 → 資料生成**のパイプラインを
内製すれば、顧客ごとの専用提案資料を毎回出せる。座標だけは自動レイアウトか業種別の型を使う。

---

## サイトを触るときの注意

- `lab/` 配下は検証用。`noindex` を入れ、`sitemap.xml` には追加しない。
- `sitemap.xml` の `lastmod` は `index.html` の `dateModified` と一致させる（正直な申告）。
- `robots.txt` はAI検索・学習系ボットを意図的に許可している。方針変更の指示がない限り触らない。
