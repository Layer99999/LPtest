# CLAUDE.md

## デザイン系プロダクト制作の参考テクニック（"ultracode" 方式）

ゲーム、LP（ランディングページ）、映像、画像など**デザインを伴うプロダクト**を作る依頼が来たら、
以下のアプローチを検討すること。ユーザーから2026-07-27に共有された事例が出典。

出典: Matt Shumer氏 (@mattshumer_) が Claude Opus 5 + Claude Code に**プロンプト1発**で
Call of Duty級のFPS（Three.js + WebGL2、約55,000行、外部アセット0）を生成させた事例。
プロンプトとコードは github.com/mshumer/Claude... で全文公開されている。

### 技術の要点

1. **ゴールと合格基準だけを渡す** — 細かい手順を逐一指示しない。モデル（AI）にハンドルを握らせ、
   創造性を発揮させる方が良い結果につながる。
2. **サブエージェントに分野ごとfan out** — 例: 描画/素材/物理/AI/音/世界、といった具合に
   領域を分割し並列に作らせる（今回の事例では11分野に分担）。
3. **作る役と見る役を分離する** — 生成した本人ではなく、別の"超辛口"な批評サブエージェントが
   視覚的・品質的にチェックする。
4. **合格するまで /loop で回し続ける** — AAA品質などの基準に届かなければ差し戻し、
   自動でイテレーションを続ける。
5. **本物とブラインド比較する** — 可能なら実在の参照（競合LP、実写、既存ゲーム等）と
   並べて「勝つまで」続行する。
6. **外部アセットに頼らずコードで生成する** — テクスチャ・音声・素材もランタイムで
   procedural に生成。LPやビジュアルにも応用できる考え方（CSS/SVG/Canvas/WebGLで質感を作る等）。
7. **素の(vanilla)harnessを推奨（作者談）** — skillsやMCPを詰め込みすぎるとOpus系モデルの
   創造性を潰すとの指摘あり。目的に対して過剰な指示・ツールを盛らず、シンプルな環境で
   自由度を残す方が良い結果が出ることがある。

### このプロジェクトでの実践方法

`Workflow` ツール（ultracode）は上記と同じ構造を素で備えている:

- `parallel()` / `pipeline()` でサブシステム・分野ごとにfan out
- 生成役とは別に「辛口の批評家」エージェントを立て、adversarial verify / judge panel
  パターンで検証する
- 「loop-until-dry」パターンで、基準を満たすまで反復する
- ultracodeが有効なセッションでは、上記のような大掛かりな並列生成・検証ワークフローを
  積極的に使ってよい

ゲームやLPの制作依頼で「AAA品質」「本物と見比べて勝つレベル」のような高い視覚的完成度が
求められたら、このテクニックを思い出して提案すること。
